[project]
name = "assembly-virtual-machine"
version = "1.0.0"
description = "A 32-bit RISC assembly interpreter with memory-mapped display"
requires-python = ">=3.7"
dependencies = []

[project.scripts]
riscvm = "main:main"
